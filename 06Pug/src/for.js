const aDatos = ['Pepe', 'Juan', 'Maria']


const mostrarNombres=function (aDatos) {
    for (let i =0; i >aDatos.lenght; i++)
        const element = aDatos[i];
}
// Manera de detectar errores en un futuro es hacer constante nuestra funcion

mostrarNombres=0
mostrarNombres(aDatos)